Hello <%= @user.first_name %>,

It looks like your <%= @grocery.name %> is set to expire in two days. Make sure you use it before it goes bad! Check out fridge-butler.herokuapp.com for some recipes that include <%= @grocery.name %>s.
